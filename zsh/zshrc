. ~/.conf/common_shell/rc.sh

. $_shellpath/set-options.zsh
. $_shellpath/alias.zsh
. $_shellpath/complete.zsh
. $_shellpath/keybinding.zsh
. $_shellpath/precmd.zsh

msource ~/.options.post

# Make sure every entry in $fpath is unique.
typeset -U fpath
typeset -U path
